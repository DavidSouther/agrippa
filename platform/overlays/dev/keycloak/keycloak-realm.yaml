# Wave 5: the declarative KeycloakRealmImport for the `agrippa` dev realm
# (spec lands Step 4).
apiVersion: k8s.keycloak.org/v2beta1
kind: KeycloakRealmImport
metadata:
  name: agrippa
  namespace: keycloak
  annotations:
    argocd.argoproj.io/sync-wave: "5"
