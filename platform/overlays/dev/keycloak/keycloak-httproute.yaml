# Wave 5: routes the Keycloak dev host through the shared Gateway to
# keycloak-service (spec lands Step 4).
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: keycloak
  namespace: keycloak
  annotations:
    argocd.argoproj.io/sync-wave: "5"
