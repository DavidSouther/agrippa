# Wave -10: the forgejo Namespace (full content now -- a Namespace has no
# spec, matching keycloak/operator/namespace.yaml's own precedent).
apiVersion: v1
kind: Namespace
metadata:
  name: forgejo
  annotations:
    argocd.argoproj.io/sync-wave: "-10"
