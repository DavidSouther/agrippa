# The `resume` component of the `workloads` layer: davidsouther.com, served
# from the resume:dev image (scripts/workloads-build.sh).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml
