# Step 0 skeleton (name-only stub, no spec); real content lands Step 2.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: resume
  namespace: resume
  annotations:
    argocd.argoproj.io/sync-wave: "0"
