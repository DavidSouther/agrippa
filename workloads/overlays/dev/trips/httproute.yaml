# Step 0 skeleton (name-only stub, no spec); real content lands Step 3.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: trips
  namespace: trips
  annotations:
    argocd.argoproj.io/sync-wave: "0"
