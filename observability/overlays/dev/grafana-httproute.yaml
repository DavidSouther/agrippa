# Wave 5: routes the shared Gateway to Grafana. Spec lands Step 4.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: observability
  annotations:
    argocd.argoproj.io/sync-wave: "5"
