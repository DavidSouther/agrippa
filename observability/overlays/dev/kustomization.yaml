# Empty-but-valid placeholder: apps/observability.yaml's Application syncs
# this path and must reach zero-resource Synced/Healthy trivially until a
# later feature-step (loki, grafana, tempo, mimir, alloy) lands real content.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
