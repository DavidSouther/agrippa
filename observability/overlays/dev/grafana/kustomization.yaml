# Wave 5: Grafana (SQLite + local-path PVC, explicit admin/admin dev
# credential, datasources). Chart lands Step 3.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"
helmCharts: []
