# Wave 0: metallb's IP pool and L2 advertisement. Real specs (addresses,
# ipAddressPools) land Step 3.
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: agrippa-pool
  namespace: metallb-system
  annotations:
    argocd.argoproj.io/sync-wave: "0"
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: agrippa-l2
  namespace: metallb-system
  annotations:
    argocd.argoproj.io/sync-wave: "0"
