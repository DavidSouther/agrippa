# Wave -10: substrate CRDs other wave -5 controllers (Istio, cert-manager)
# watch at startup. Gateway API standard-install.yaml is a single pinned
# upstream manifest -- URL lands Step 1.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonAnnotations:
  argocd.argoproj.io/sync-wave: "-10"
resources: []   # pinned Gateway API standard-install.yaml URL lands Step 1
