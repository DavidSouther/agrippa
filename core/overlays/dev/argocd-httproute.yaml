# Wave 5: routes the ArgoCD UI host through the shared Gateway to
# argocd-server. Real spec lands Step 4.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "5"
