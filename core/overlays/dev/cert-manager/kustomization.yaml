# Wave -5: cert-manager's static install (bundles its own CRDs, controller,
# and webhook). Pinned cert-manager.yaml manifest URL lands Step 2.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonAnnotations:
  argocd.argoproj.io/sync-wave: "-5"
resources: []   # pinned cert-manager.yaml URL lands Step 2
